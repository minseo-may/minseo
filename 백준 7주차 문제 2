#pragma warning(disable:4996)
#include <stdio.h>
#include <stdlib.h>

int main() {
	int ar[31] = { 0 };//모두 0으로 초기화
	for (int i = 1;i <=28;i++) {
		int n;
		scanf("%d", &n);
		ar[n] = 1;
	}
	for (int i = 1;i <= 30;i++) {
		if (ar[i] == 0) {//제출하지 않았다면
			printf("%d\n", i);
		}
	}
	return 0;
}
