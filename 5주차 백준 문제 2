#pragma warning(disable:4996)
#include <stdio.h>
#include <stdlib.h>



int main() {
	int N;
	scanf("%d", &N);//실행횟수 
	int a, b;
	for (int i = 1;i <= N;i++) {
		scanf("%d %d", &a, &b);
		printf("Case #%d: %d + %d = %d\n", i, a, b, a + b);
	}
	return 0;
}
